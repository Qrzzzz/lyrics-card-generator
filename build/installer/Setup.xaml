<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
 Title="Lyrics Card Generator · Setup" Width="460" Height="354" MinWidth="460" MinHeight="354"
 WindowStyle="None" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen"
 Background="Transparent" FontFamily="Segoe UI Variable Text, Microsoft YaHei UI, Segoe UI"
 FontSize="13" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
 <shell:WindowChrome.WindowChrome>
  <shell:WindowChrome CaptionHeight="54" ResizeBorderThickness="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
 </shell:WindowChrome.WindowChrome>
 <Window.Resources>
  <SolidColorBrush x:Key="Ink" Color="#F8FAFC"/>
  <SolidColorBrush x:Key="Muted" Color="#BBC5D6"/>
  <SolidColorBrush x:Key="Line" Color="#25FFFFFF"/>
  <SolidColorBrush x:Key="Surface" Color="#0CFFFFFF"/>
  <SolidColorBrush x:Key="Hover" Color="#22FFFFFF"/>
  <SolidColorBrush x:Key="Accent" Color="#C4B5FD"/>
  <SolidColorBrush x:Key="AccentInk" Color="#201738"/>
  <Style TargetType="TextBlock">
   <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
   <Setter Property="TextWrapping" Value="Wrap"/>
  </Style>
  <Style x:Key="Focus" TargetType="Control">
   <Setter Property="Template"><Setter.Value><ControlTemplate>
    <Border BorderBrush="{DynamicResource Accent}" BorderThickness="2" CornerRadius="6" Margin="-3"/>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style TargetType="Button">
   <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
   <Setter Property="Background" Value="{DynamicResource Surface}"/>
   <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
   <Setter Property="BorderThickness" Value="1"/>
   <Setter Property="Padding" Value="16,10"/>
   <Setter Property="Cursor" Value="Hand"/>
   <Setter Property="FocusVisualStyle" Value="{StaticResource Focus}"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
    <Border x:Name="Box" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
     <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
    </Border>
    <ControlTemplate.Triggers>
     <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Box" Property="Opacity" Value="0.8"/></Trigger>
     <Trigger Property="IsPressed" Value="True"><Setter TargetName="Box" Property="Opacity" Value="0.6"/></Trigger>
     <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
    </ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style TargetType="TextBox">
   <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
   <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
   <Setter Property="CaretBrush" Value="{DynamicResource Accent}"/>
   <Setter Property="SelectionBrush" Value="{DynamicResource Accent}"/>
   <Setter Property="Background" Value="{DynamicResource Surface}"/>
   <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
   <Setter Property="Padding" Value="10,8"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TextBox">
    <Border x:Name="Box" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
     <ScrollViewer x:Name="PART_ContentHost" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
    </Border>
    <ControlTemplate.Triggers><Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent}"/></Trigger></ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style TargetType="CheckBox">
   <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
   <Setter Property="Cursor" Value="Hand"/>
   <Setter Property="FocusVisualStyle" Value="{StaticResource Focus}"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="CheckBox">
    <Grid Background="Transparent"><Grid.ColumnDefinitions><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <Border x:Name="Box" Width="17" Height="17" CornerRadius="5" BorderThickness="1" BorderBrush="{DynamicResource Muted}" Background="{DynamicResource Surface}">
      <Path x:Name="Tick" Data="M 3,7 L 6,10 L 12,4" Stroke="{DynamicResource AccentInk}" StrokeThickness="1.8" Visibility="Collapsed"/>
     </Border>
     <ContentPresenter Grid.Column="1" Margin="9,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
    </Grid>
    <ControlTemplate.Triggers>
     <Trigger Property="IsChecked" Value="True"><Setter TargetName="Box" Property="Background" Value="{DynamicResource Accent}"/><Setter TargetName="Tick" Property="Visibility" Value="Visible"/></Trigger>
     <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent}"/></Trigger>
    </ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
 </Window.Resources>
 <Border x:Name="WindowSurface" CornerRadius="12" BorderBrush="{DynamicResource WindowLine}" BorderThickness="1">
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/></Grid.RowDefinitions>
   <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,0" shell:WindowChrome.IsHitTestVisibleInChrome="True">
    <Button x:Name="LanguageButton" Content="EN" FontSize="12" BorderThickness="0" Background="Transparent" Padding="12,6" Margin="0,7,6,7"/>
    <Button x:Name="MinimizeButton" BorderThickness="0" Background="Transparent" Width="36" Padding="10"><Path Data="M 0,5 H 12" Stroke="{DynamicResource Ink}" StrokeThickness="1"/></Button>
    <Button x:Name="CloseButton" BorderThickness="0" Background="Transparent" Width="36" Padding="10"><Path Data="M 1,1 L 11,11 M 11,1 L 1,11" Stroke="{DynamicResource Ink}" StrokeThickness="1"/></Button>
   </StackPanel>
   <Grid Grid.Row="1" Margin="30,20,30,26">
    <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="56"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <Image x:Name="BrandIcon" Width="52" Height="52" AutomationProperties.Name="Lyrics Card Generator" RenderOptions.BitmapScalingMode="HighQuality"/>
     <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
      <TextBlock Text="{DynamicResource ProductName}" FontSize="18" FontWeight="SemiBold"/>
      <TextBlock x:Name="VersionLabel" Foreground="{DynamicResource Muted}" FontSize="12" Margin="0,5,0,0"/>
     </StackPanel>
    </Grid>
    <Grid Grid.Row="1" Margin="0,28,0,0">
     <StackPanel x:Name="ReadyPanel">
      <TextBlock Text="{DynamicResource Location}" Foreground="{DynamicResource Muted}" FontSize="12"/>
      <TextBlock Text="{Binding Text, ElementName=InstallPath}" ToolTip="{Binding Text, ElementName=InstallPath}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,7,0,0" FontSize="12"/>
      <Button x:Name="OptionsButton" Content="{DynamicResource Options}" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Padding="0,9" Margin="0,4,0,0" Foreground="{DynamicResource Muted}" AutomationProperties.Name="{DynamicResource Options}"/>
      <StackPanel x:Name="OptionsPanel" Visibility="Collapsed" Margin="0,8,0,14">
       <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="38"/></Grid.ColumnDefinitions>
        <TextBox x:Name="InstallPath" FontSize="12" Height="38" AutomationProperties.AutomationId="InstallPath"/>
        <Button x:Name="BrowseButton" Grid.Column="1" Content="…" Margin="6,0,0,0" Padding="0" FontSize="18"/>
       </Grid>
       <CheckBox x:Name="AllUsers" Margin="0,20,0,16"><TextBlock Text="{DynamicResource AllUsers}" FontSize="13"/></CheckBox>
       <CheckBox x:Name="DesktopShortcut" IsChecked="True"><TextBlock Text="{DynamicResource Shortcut}" FontSize="13"/></CheckBox>
      </StackPanel>
      <Button x:Name="InstallButton" Content="{DynamicResource Install}" Background="{DynamicResource Accent}" Foreground="{DynamicResource AccentInk}" BorderThickness="0" FontSize="14" FontWeight="SemiBold" Height="42" Margin="0,16,0,0" AutomationProperties.AutomationId="Install"/>
     </StackPanel>
     <StackPanel x:Name="WorkingPanel" Visibility="Collapsed">
      <TextBlock x:Name="WorkingStatus" Text="{DynamicResource WorkingTitle}" FontSize="15" Margin="0,8,0,0"/>
      <ProgressBar x:Name="InstallProgress" Height="3" Margin="0,20,0,18" IsIndeterminate="True" Foreground="{DynamicResource Accent}" Background="{DynamicResource Surface}" BorderThickness="0"/>
      <TextBlock Text="{DynamicResource WorkingHint}" FontSize="12" Foreground="{DynamicResource Muted}" LineHeight="19"/>
     </StackPanel>
     <StackPanel x:Name="DonePanel" Visibility="Collapsed">
      <TextBlock Text="{DynamicResource DoneTitle}" FontSize="15" Margin="0,8,0,66"/>
      <TextBlock x:Name="DonePath" Visibility="Collapsed"/>
      <Button x:Name="LaunchButton" Content="{DynamicResource Launch}" Background="{DynamicResource Accent}" Foreground="{DynamicResource AccentInk}" BorderThickness="0" Height="42" FontSize="14" FontWeight="SemiBold"/>
      <Button x:Name="FinishButton" Content="{DynamicResource Close}" Background="Transparent" BorderThickness="0" Padding="12,7" Margin="0,6,0,0" HorizontalAlignment="Center" Foreground="{DynamicResource Muted}"/>
     </StackPanel>
     <Border x:Name="MessagePanel" Visibility="Collapsed" VerticalAlignment="Top">
      <StackPanel><TextBlock x:Name="MessageText" LineHeight="21"/><Button x:Name="DismissButton" Content="{DynamicResource Dismiss}" HorizontalAlignment="Right" Margin="0,18,0,0"/></StackPanel>
     </Border>
    </Grid>
   </Grid>
  </Grid>
 </Border>
</Window>
