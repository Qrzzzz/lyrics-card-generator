# 多语言发布说明规范

Lyrics Card Generator 面向国内用户，GitHub Release 页面默认使用简体中文。其他语言不直接堆在 Release 正文里，而是放在 `docs/releases/` 目录下维护，再从 Release 正文跳转过去。

Release 页面保持短、清楚；完整说明保留 6 种语言，方便不同用户阅读。

## 语言文件命名

每个正式版本分别维护 6 个 Markdown 文件：

```text
docs/releases/vX.Y.Z.zh-CN.md
docs/releases/vX.Y.Z.zh-TW.md
docs/releases/vX.Y.Z.en.md
docs/releases/vX.Y.Z.fr.md
docs/releases/vX.Y.Z.ja.md
docs/releases/vX.Y.Z.es.md
```

当前项目界面支持：简体中文、繁體中文、English、Français、日本語、Español。

## 已整理版本

- `v0.1.0`：Windows 桌面版初版发布。
- `v1.0.0`：Windows 桌面正式版、检查更新、自动高度和导出体验优化。
- `v1.1.0`：多语言发布说明待迁移补齐。
- `v2.0.0`：横版布局、本地音频、系统字体、导出与更新检查增强。
- `v2.1.0`：多语言界面与文档更新。
- `v3.0.0`：歌词翻译、设置中心与导出流程优化。
- `v3.2.1`：v3.2.0 的 bugfix 版本，修复主题可读性、首次语言持久化、Header 清空入口与背景设置稳定性，并补充细网格控制。
- `v3.3.0`：新增 Spotify 单曲链接解析，支持自动填入标题、歌手、封面并显示平台 Logo。
- `v3.3.1`：v3.3.0 的 bugfix 版本，修复卡片背景细网格叠加和关闭后仍残留网格的问题。
- `v3.4.0`：重构布局步骤的内容类型逻辑，将背景网格升级为可配置的视觉细节选项，锁定纯音乐卡片为 1:1。
- `v3.6.0`：新增深色亚克力与浅色亚克力应用主题，Windows 桌面端支持真正的 Acrylic 半透明窗口材质，并升级为融合式自绘标题栏与内部滚动条。
- `v3.6.1`：修复 Acrylic 桌面标题栏最大化/还原状态同步、hover、层级和多语言文案，并修正当前下载入口。
- `v3.7.1`：界面控件标准化版本，统一开关、选项卡片、分段控件与设置面板基础控件，并改善可访问性与焦点反馈，为后续 v3.8.0 动效升级打基础。
- `v3.8.0`：集中化动效系统版本，统一编辑器、设置、预览外壳、共享控件、AI 面板与应用背景的动效，并保持导出 PNG 静态输出稳定。
- `v3.8.1`：维护性重构版本，拆分 LyricEditor 职责、整理 AI provider 请求辅助逻辑，并在不改变用户可见行为的前提下同步完成类型检查、回归测试和桌面构建验证。
- `v3.9.0`：设置即时保存与卡片信息改进版本，保持原有六语言范围，并改进手动歌曲信息、专辑名显示与 Windows 发布产物。
- `v3.9.1`：桌面窗口圆角与标题栏修复版本，改用 Windows 原生圆角裁剪，限制背景与标题栏在应用外壳内绘制，并改用红黄绿窗口控制按钮。
- `v4.0.0`：新增网易云音乐歌名搜索作为第一选歌入口，支持实时候选、键盘选择和选中后自动导入歌曲信息与可用歌词，同时保留旧的链接与本地音频入口。
- `v4.1.0`：重构样式面板的容器层级，让字体方案保留重点质感，普通布局、颜色、背景和细节设置改为连续设置行，并恢复布局模式等横向选择控件的原有呈现。
- `v4.1.5`：修复主页左侧 Stepper 的自适应高度与均衡换行，限制步骤区只在 `6`、`3 + 3`、`2 + 2 + 2` 三种结构中切换，并避免一行布局出现过宽空白。
- `v4.2.0`：升级示例歌词系统为原语言歌词加五种目标语言翻译样例，并修复 Web Lite / GitHub Pages 的图标路径与 artifact 准备方式。
- `v4.2.3`：收敛软件界面外观系统，移除独立背景设置页，将界面外观固定为专辑封面动态取色、深色、浅色、深色亚克力和浅色亚克力五种模式。
- `v4.2.6`：建立统一 App Accent 系统，新增预设/自定义主题色，拆分界面模式与亚克力效果，并将导出质量收敛为低、标准、高清三档。
- `v4.3.0`：示例歌曲对话框重构为响应式网格，翻译跟随界面语言匹配，新增显式标志渲染，并收紧内容类型、文字颜色、视觉细节与最终导出控件。
- `v4.3.2`：打磨 Explicit 标识为 SVG 镂空几何图形，微调标题对齐与间距，并让竖版封面和歌曲信息形成更稳定的视觉中心。
- `v4.3.4`：将“歌词示例”从弹窗改为主界面上方空间层，复用应用内标题栏作为底部返回锚点，并让示例卡片按主窗口宽度自适应布局。
- `v4.3.6`：修复示例歌曲界面与主编辑标题栏的衔接，新增“导入翻译”联动开关，整理简繁中文示例导入逻辑，并将导出质量文案调整为标准、高清、超清。
- `v4.3.8`：示例歌曲界面改为更紧凑的本地取色遮罩卡片，预览图复用主歌词卡片渲染链路生成，真实专辑封面只作为临时取色输入，并新增 `docs/examples.md` 维护流程。
- `v4.3.9`：将示例歌曲重构为整张可点击的连续专辑配色卡片，展示真实曲目元数据、自动适配明暗文字，并以封面调色板元数据替代生成的本地预览图。
- `v4.4.0`：将设置从模态弹窗迁移为编辑器右侧的完整空间层，新增三档响应式导航、统一表面互斥与焦点行为，同时保留即时保存和 AI 延迟保存语义。
- `v4.4.3`：统一大多数按钮的悬浮动效，修复小屏设置分类的 Escape 与重开生命周期，引入 AI 设置四态保存反馈，并优化设置外框、导出质量控件、窗口圆角和示例页关闭按钮。
- `v4.4.6`：统一六步 Stepper 与底部导航留白，重组第三至第五步文字、尺寸和布局控件，优化设置与标题栏视觉，并改进第六步导出排版和 PNG 生成动画。
- `v4.5.0`：优化纯音乐卡片的歌曲信息排版，恢复紧凑的平面窗口控制按钮，为设置分区加入切换动效与减弱动效开关，并扩展水印和分享人导出默认值。
- `v4.6.0`：重构 AI 提示词为可管理的资源库，保护推荐版与严格格式规则，开放其余默认预设编辑，并支持最多两个自定义预设。
- `v4.6.2`：修复桌面端文本输入，加入单个预设与整库重置，调整最大化提示文案，并让实时预览边框随卡片高度自适应。
- `v4.6.4`：修复设置重置后的全局交互阻塞，锁定严格格式规则，重构设置导航，固定真实预览，并精简字体方案与示例卡片。
- `v5.0.0`：将前两步重构为专注式歌曲导入与三栏长歌词工作台，加入三态编辑视口、36 行导出保护和独立离屏导出宿主。
- `v5.0.2`：在保持 v5.0.0 交互与功能不变的前提下统一发布版本面，更新兼容范围内的运行时依赖并重建 Windows 与 Web Lite 产物。
- `v5.0.4`：继承固定搜索结果面板，固定第二步最大编辑高度，重排设置导航与 AI 工作区，并加入两首新示例和两行滚动示例库。
- `v5.1.0`：安全与稳定性版本，加入逐跳 Safe Fetch、原子导入事务、不可变导出快照，并统一桌面版与 Web Lite 的最低导出安全契约。
- `v5.2.0`：稳定性、可访问性、Electron 安全与发布工程版本，加入多地址 Safe Fetch、关闭前持久化、六语错误码、最终制品 smoke、SBOM 与 attestation。
- `v5.2.1`：歌曲链接解析恢复版本，修复 QQ 音乐数值 songDetail、网易云 JSON 元数据和 Apple Music 首页跳转后的曲目解析，同时保留既有 Safe Fetch 安全边界。
- `v5.2.2`：修复发布标签校验、Stepper 勾选状态、设置面包屑导航样式，以及示例歌曲替换后实时预览尺寸异常。
- `v5.2.3`：修复同标签下已发布与草稿 Release 的附件解析歧义，改为按唯一 Release ID 校验并发布完整制品。
- `v5.3.0`：统一第 2 至第 6 步的紧凑 Stepper 与顶部操作区，移除重复的大型标题栏，并在 Windows 窗口条加入小型应用图标。
- `v5.4.0`：将第一步纳入统一紧凑 Stepper 与双栏工作台，合并歌曲信息和其他导入方式，同时保持既有导入与后续编辑行为不变。
- `v5.5.0`：为竖版自定义歌词卡加入基于真实排版的可选自动宽度，同时检测正文与翻译孤行，并与自动高度和导出安全单向协调。
- `v5.5.1`：新建竖版自定义歌词卡默认开启自动宽度，统一行高范围，并加固候选完整性、内容测量、样例校准与跨端回归测试。
- `v5.5.2`：重排第一步的搜索、链接、本地音频与歌曲信息区域，移除首步无效返回按钮，并以细分隔线替代第二步的嵌套外框。
- `v5.5.3`：让字体方案卡真实展示对应字体，加入轻量标题栏渐进模糊，移动第一步手动调整入口，并为浅色亚克力建立对比度下限。
- `v5.5.4`：加固本地应用 POST 请求边界，统一六语言产品文案并补齐 Web Lite 六语言支持，同时沿用共享 CI 与发布工作流。
- `v5.6.0`：重制“歌词示例”外框与单层窗口边缘渐变，并将第一步“手动调整”改为草稿保存/取消和双向滑动切换。
- `v5.6.1`：修复 v5.5.4 请求边界误将浏览器的 `127.0.0.1` 与 Next 内部 `localhost` 判为跨域，恢复网易云搜索、结果解析及多平台歌曲链接解析。
- `v5.6.2`：为第 3 至第 6 步加入可降级的 3D 预览反馈，以三面板整体平移衔接第 5、6 步，并让桌面版与 Web Lite 支持 PNG、WebP、JPG 导出及默认格式持久化。
- `v5.7.0`：重构桌面端六步工作台，为第 3–5 步加入 1/2–2/3 可调设置/预览分栏与容器驱动的紧凑排版，并延续 PNG、WebP 与 JPG 导出。
- `v5.7.1`：精简可调分隔器视觉，并让第 5→6 步的预览从任意分栏宽度同步平滑放大到等分半宽。
- `v5.7.3`：将第二步重构为编辑优先的可调双栏工作台，加入可折叠工具轨、紧凑粘性状态栏与连续共享滚动编辑。
- `v5.8.0`：完成第二步歌词工作区整轮重构，加入窄命令栏、四个稳定侧栏 Tab、选区优先的可撤销文本处理、双栏同步安全检查与窄屏覆盖式抽屉。
- `v5.8.1`：重排第二步命令栏与侧栏信息架构，将歌词获取和检查移至顶部独立入口，移除查找替换，并直接展开单入口工具区。
- `v5.8.2`：将第二步视觉改为透明画布上的轻量工作区，仅保留歌词编辑面和关键反馈面板的明确边界，同时维持现有功能分区与交互。
- `v5.8.3`：统一第二步整理／翻译侧栏与全局设置的控件语言，改善分组、字号和滚动边界，并移除会遮挡长内容的黏性范围区。
- `v5.8.4`：为第二步右侧工具栏加入连续展开／折叠动画，并让第 3–5 步的设置面板按前进或返回方向平移渐变，同时保持右侧预览稳定。
- `v5.8.5`：将第二步固定为桌面端 2:1 歌词／工具分栏，把状态集中到顶部命令栏，并以紧凑范围、常用／更多整理和栏位工具重组右侧侧栏。
- `v5.8.6`：将第二步翻译工具中的 AI 翻译从首页内联展开提升为可反向返回的独立二级滑动页面，同时保持顶部 Tab、侧栏外框、歌词编辑区与 AI 功能不变。
- `v5.8.7`：统一第二步整理／翻译的横向页面切换，并让 AI 翻译启动后的状态、推理流和译文流进入独立的执行／结果滑动页面。

旧的 `docs/release-notes-v*.md` 单文件发布说明已迁移到本目录，避免同一版本存在多套入口。

## GitHub Release 正文写法

Release 正文只写简体中文短版。Release 页面不在仓库目录中，不能使用 `./vX.Y.Z.en.md` 这类相对链接。建议使用完整 GitHub 链接。

```md
# Lyrics Card Generator vX.Y.Z

语言：简体中文 · [繁體中文](https://github.com/Qrzzzz/lyrics-card-generator/blob/main/docs/releases/vX.Y.Z.zh-TW.md) · [English](https://github.com/Qrzzzz/lyrics-card-generator/blob/main/docs/releases/vX.Y.Z.en.md) · [Français](https://github.com/Qrzzzz/lyrics-card-generator/blob/main/docs/releases/vX.Y.Z.fr.md) · [日本語](https://github.com/Qrzzzz/lyrics-card-generator/blob/main/docs/releases/vX.Y.Z.ja.md) · [Español](https://github.com/Qrzzzz/lyrics-card-generator/blob/main/docs/releases/vX.Y.Z.es.md)
```

## 内容同步原则

6 个语言版本应保持同一结构和同一信息量。不要让英文版比中文版多信息，也不要让其他语言只剩一句“见中文版”。发布说明面向用户，不要写“给发布者的备注”“Maintainer Note”这类内部说明。

涉及目标语言的功能描述必须本地化。例如 v3.0.0 的歌词翻译功能：中文用户看到的是中文歌词翻译，英文用户看到的是 English lyric translations，法语用户看到的是 traduction française，日本语用户看到的是 日本語の歌詞翻訳，西语用户看到的是 traducción al español。

也就是说，目标译文语言应跟随当前界面语言，不能所有语言版本都写成“生成中文翻译”。

## 工作流程

1. 先构建版本并确认版本号。
2. 复制 `.github/RELEASE_TEMPLATE.md`，填好中文短版 Release 正文。
3. 在 `docs/releases/` 下补齐 6 个语言文件。
4. 创建 Git tag 和 GitHub Release。
5. 添加发布产物。
6. 发布后检查应用内“检查更新”能否正确识别最新版。
