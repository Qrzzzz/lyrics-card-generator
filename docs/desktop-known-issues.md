# Desktop Known Issues

No desktop-specific runtime issues are currently confirmed.

Validation notes:

- The installer EXE was built but not installed on this machine to avoid modifying the user's installed applications.
- The portable EXE and unpacked EXE were both runtime-tested.
- Remote parsing and LRCLIB fetching depend on the availability of their upstream data sources.
